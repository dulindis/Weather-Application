<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="{{title}}">Weather App - {{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <main class="wrapper">
        {{>header}}
        <div class="flex-grow">
            <h2 class="incorrect">{{title}}</h2>
            <h3 class="heading-3 incorrect">{{error}}</h3>
        </div>
        {{>footer}}
    </main>
</body>

</html>